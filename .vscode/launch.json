{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "run client",
            
            "type": "f5anything",
            "request": "launch",

            "command": "./gradlew runClient"
        }
    ]
}